# 历史本地参考

这个文件现在只保留为轻量说明。之前那种很长的本地快照已经移除，避免和当前主文档重复、过时。

## 优先查看

- 主提示词：`prompts/api-test-restassure.md`
- 主入口：`SKILL.md`

## 说明

- 以后请以当前提示词和 `SKILL.md` 为准。
- 只有在任务真的需要补充信息时，再按需打开 `references/`、`examples/` 或 `scripts/`。
- 不要再把这个文件当成最新提示词或最新流程说明。
